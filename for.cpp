
#include <iostream> //Se manda a llamar una libreria 
using namespace std; //Se usa el STD para poder liberar espacio de codigo

int main() { // Función que se esta solucionando

    for(int i = 1; i <= 5; i++) { //For es una funcion en donde dentro esta la cnodicon a  cumplir
        cout << "Valor de i: " << i << endl; // Se imprime el mensaje que va ir tomando con la repiticón
    }

    return 0;
}
